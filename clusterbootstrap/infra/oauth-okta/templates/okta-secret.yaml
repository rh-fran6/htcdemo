apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: okta-oauth-secret
  namespace: openshift-config               
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secret-manager
  target:
    name: okta-login-secret           
    creationPolicy: Owner
    template:
      type: Opaque
  data:
    - secretKey: okta_secret  
      remoteRef:
        key: secrets/okta_secret  
        property: password
    - secretKey: github_secret  
      remoteRef:
        key: secrets/github_secret  
        property: password