apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nginx
  namespace: {{ .Values.namespace }}
spec:
  to:
    kind: Service
    name: nginx
  port:
    targetPort: http
  tls:
    termination: edge